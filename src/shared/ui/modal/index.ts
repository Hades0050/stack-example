export { default as Modal } from './Modal.vue';
export type {
  IModalProps,
  IModalEmits,
  ModalProps,
  ModalResolve,
  ModalCallback,
  VoidCallback,
  ModalOptions,
  ModalState,
} from './types';
export { useModal, globalModal } from './use-modal';

